// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "app.vwt.wallet"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
